import { Magic } from 'magic-sdk'

//FIXME: if magicClient is used in more than just login.tsx
// export const magicClient = new Magic(
//   process.env.NEXT_PUBLIC_MAGIC_PUBLISHABLE_KEY || '',
//   { testMode: process.env.STAGING === '1' }
// )